# 🎲 Fake Data Generator

<div align="center">

![Python](https://img.shields.io/badge/Python-3.7+-blue.svg)
![License](https://img.shields.io/badge/License-MIT-yellow.svg)

**Генератор фейковых данных на русском языке с экспортом в CSV, JSON и TXT**

[Описание](#-описание) • [Возможности](#-возможности) • [Установка](#-установка) • [Использование](#-использование) • [Примеры](#-примеры)

</div>

---

## 📋 Описание

`fake-data` — это простой и удобный Python-скрипт для генерации реалистичных фейковых данных на русском языке. Скрипт использует библиотеку Faker для создания тестовых данных и сохраняет их в нескольких форматах: CSV, JSON и TXT.

Идеально подходит для:
- 🧪 Создания тестовых данных для разработки
- 📊 Заполнения базы данных демо-данными
- 🎯 Тестирования приложений с реалистичными данными
- 📈 Демонстрации функционала приложений

## ✨ Возможности

- 🇷🇺 **Русская локализация** — все данные генерируются на русском языке
- 📝 **Множество форматов** — экспорт в CSV, JSON и TXT
- 🚀 **Высокая производительность** — эффективная генерация больших объемов данных
- ⚙️ **Гибкая настройка** — настраиваемое количество записей через аргументы командной строки
- 📊 **Богатый набор полей** — ID, имя, компания, должность, email, IP-адрес, дата регистрации, описание
- ✅ **Обработка ошибок** — понятные сообщения об ошибках

## 🚀 Установка

### Требования

- Python 3.7 или выше
- pip (менеджер пакетов Python)

### Шаги установки

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/CHUKEPC/fake-data-generator.git
cd fake-data
```

2. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

## 💻 Использование

### Базовое использование

Сгенерировать 1000 записей (значение по умолчанию):

```bash
python main.py
```

### Указание количества записей

Сгенерировать определенное количество записей:

```bash
python main.py --count 5000
```

или

```bash
python main.py -c 5000
```

### Указание имени выходного файла

Изменить имя базового файла для сохранения:

```bash
python main.py --output my_data
```

или

```bash
python main.py -o my_data
```

### Комбинирование параметров

```bash
python main.py --count 10000 --output large_dataset
```

### Справка

Посмотреть все доступные опции:

```bash
python main.py --help
```

## 📖 Примеры

### Пример 1: Генерация 100 записей

```bash
python main.py -c 100
```

**Результат:**
- Создана папка `output/` (если её нет)
- Файлы сохранены:
  - `output/test_data.csv`
  - `output/test_data.json`
  - `output/test_data.txt`
- Вывод в консоль:
```
Запуск генерации 100 строк...
Подготовка данных завершена за: 0.15 сек.
Файлы успешно сохранены в папку output/
```

### Пример 2: Генерация большого объема данных

```bash
python main.py --count 100000 --output large_dataset
```

**Результат:**
- Созданы файлы с 100,000 записями:
  - `output/large_dataset.csv`
  - `output/large_dataset.json`
  - `output/large_dataset.txt`

### Пример 3: Использование в Python-коде

```python
from main import generate_data_pandas, save_all_formats

# Генерация данных
df = generate_data_pandas(num_records=500)

# Сохранение в разных форматах
save_all_formats(df, base_name="custom/path/my_data")
```

## 📊 Формат данных

Скрипт генерирует следующие поля:

| Поле | Описание | Пример |
|------|----------|--------|
| ID | Уникальный числовой идентификатор | 123456 |
| Имя | Полное имя (ФИО) | Иванов Иван Иванович |
| Компания | Название компании | ООО "Технологии" |
| Должность | Название должности | Менеджер по продажам |
| Email | Электронная почта | ivanov@example.com |
| IP-адрес | IPv4 адрес | 192.168.1.1 |
| Дата регистрации | Дата в формате YYYY-MM-DD | 2023-05-15 |
| Описание | Текстовое описание (до 100 символов) | Текст описания... |

## 🛠 Технологии

- **Python 3.7+** — основной язык программирования
- **pandas** — работа с данными и экспорт в различные форматы
- **Faker** — генерация реалистичных фейковых данных
- **argparse** — обработка аргументов командной строки (стандартная библиотека)

## 📁 Структура проекта

```
fake-data/
│
├── main.py          # Основной скрипт
├── README.md        # Документация проекта
├── requirements.txt # Зависимости проекта
├── .gitignore       # Игнорируемые файлы для Git
└── output/          # Папка с сгенерированными файлами (создается автоматически)
```

## 🔧 Параметры командной строки

| Параметр | Короткая форма | Описание | По умолчанию |
|----------|----------------|----------|--------------|
| `--count` | `-c` | Количество записей для генерации | 1000 |
| `--output` | `-o` | Базовое имя файла (без расширения) | `output/test_data` |

## ⚙️ Как это работает

1. Скрипт инициализирует Faker с русской локализацией (`ru_RU`)
2. Генерирует указанное количество записей с уникальными данными
3. Создает pandas DataFrame из сгенерированных данных
4. Сохраняет данные в трех форматах:
   - **CSV** — для работы в Excel и других табличных редакторах (кодировка UTF-8-sig)
   - **JSON** — для использования в веб-приложениях и API
   - **TXT** — для просмотра в текстовых редакторах
5. Выводит статистику о времени выполнения

## 🐛 Обработка ошибок

Скрипт обрабатывает следующие ошибки:
- **Ошибки создания директории** — автоматически создает необходимые папки
- **Ошибки записи файлов** — выводит понятные сообщения об ошибках
- **Некорректные параметры** — валидация входных данных

## ⚠️ Важные замечания

- ⚠️ **Уникальность ID** — при генерации большого количества записей (>900,000) может возникнуть конфликт уникальных ID (диапазон 100000-999999)
- ⚠️ **Память** — при генерации очень больших объемов данных (>1,000,000 записей) убедитесь, что у вас достаточно оперативной памяти
- 💡 **Производительность** — генерация 100,000 записей занимает примерно 5-10 секунд на среднем компьютере

## 🤝 Вклад в проект

Вклад в проект приветствуется! Если у вас есть идеи по улучшению:

1. Fork проекта
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Зафиксируйте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Отправьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📝 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для подробностей.

## 👤 Автор

**CHUKEPC**
- GitHub: [@CHUKEPC](https://github.com/CHUKEPC)
- Telegram: [@ArtyomRoyal](https://t.me/ArtyomRoyal)

---

<div align="center">

⭐ Если проект был полезен, поставьте звезду! ⭐

</div>
